// фигурные скобки js

// block statement
{
  let x = 42
}

// object literal expression
let o = {
  x: 42,
  key: 'foo',
  value: 'bar',
}

// template string literal expression
let s = `ваше число ${x} !`

///
///
///
///

function methodName(arguments) {
  let x = 42
}

if (true) {
  let x = 42
}
